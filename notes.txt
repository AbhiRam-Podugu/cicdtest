echo "# cicdtest" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/AbhiRam-Podugu/cicdtest.git
git push -u origin main
build a homepage for royal enfield medallion bronze bike if possible take images from google results and paste them in website and modify the  the style.css to the very next level .